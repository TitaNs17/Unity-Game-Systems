# Legacy Restaurant Scripts

These scripts come from the earlier restaurant prototype and are kept together so the original project-specific implementations stay easy to browse.

The newer reusable restaurant architecture remains in the structured `Core`, `Customers`, `Kitchen`, `Management`, `Tests` and `V2` folders.
